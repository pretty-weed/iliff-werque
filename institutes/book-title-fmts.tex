

\titleformat{name=\chapter,numberless}[display]
{\normalfont\huge\bfseries}
{}
{1em}
{}

\titleformat{\chapter}[display]
{\normalfont\huge\bfseries}
{Book \Ordinalstring{chapter}}
{1em}
{}


\titleformat{name=\section,numberless}
{\normalfont\Large\bfseries}	 	% 1. Format (shape/font)
{}                          	 	% 2. Label (empty if you want no number)
{1em}                       	 	% 3. Sep (vertical space in 'display' mode)
{}


\titleformat{\section}[display]
{\normalfont\Large\bfseries}	 	% 1. Format (shape/font)
{the \Ordinalstring{section} Chapter}      % 2. Label (empty if you want no number)
{1em}                       	 	% 3. Sep (vertical space in 'display' mode)
{} 	% 4. Code before title body

\newcommand{\marginheading}[1]{%
	\marginnote{\normalfont\itshape #1}[-4.75ex]%
}

%\titleformat{\subsection}[runin]
%{\normalfont\bfseries} % Format for the inline number (e.g., Bold)
%{\thesubsection}       % Position the number as the inline run-in anchor
%{0.5em}                % Space between the number and the start of paragraph text
%{\marginheading}
% Dynamic outside margin layout for subsections

% Set the font to be small, bold, and italicized
\renewcommand*{\marginfont}{\normalfont\small\bfseries\itshape}

% Keep this command the same to handle the outside margins safely
\RenewDocumentCommand{\subsection}{s m}{%
	\leavevmode%
	\marginnote{#2}%
}
% 3. Adjust the vertical spacing around your new subsection entries
\titlespacing*{\subsection}
{0pt}                         % Indent before the number
{3.25ex plus 1ex minus .2ex}  % Space before the paragraph starts
{0.5em}  

%\titleformat{\paragraph}[runin]
%{\normalfont\bfseries} % Format for the inline number (e.g., Bold)
%{Lib \roman{chapter}:Cap \arabic{section} \arabic{subsection}.\arabic{paragraph}}       % Position the number as the inline run-in anchor
%{0.5em}                % Space between the number and the start of paragraph text
%{\marginheading}

% 3. Adjust the vertical spacing around your new subsection entries
\titlespacing*{\paragraph}
{0pt}                         % Indent before the number
{3.25ex plus 1ex minus .2ex}  % Space before the paragraph starts
{0.5em}  

\titleformat{\subsubsection}[leftmargin]
{\normalfont\bfseries} % Format for the inline number (e.g., Bold)
{{L\textsuperscript{ib}\Roman{chapter} C\textsuperscript{ap} \arabic{section} \S\arabic{subsection}.\arabic{subsubsection}}    }       % Position the number as the inline run-in anchor
{0.5em}                % Space between the number and the start of paragraph text
{}

% 3. Adjust the vertical spacing around your new subsection entries
\titlespacing*{\subsubsection}
{0pt}                         % Indent before the number
{3.25ex plus 1ex minus .2ex}  % Space before the paragraph starts
{0.5em}  

% Section: Needs space for title + padding + 8 lines
\newcommand{\sectionbreak}{\needspace{15\baselineskip}}

% Subsection: Needs space for title + padding + 8 lines
\newcommand{\subsectionbreak}{\needspace{12\baselineskip}}

% Subsection: Needs space for title + padding + 8 lines
\newcommand{\paragraphbreak}{\needspace{12\baselineskip}}

